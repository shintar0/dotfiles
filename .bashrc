# ===== 共通設定 =====
source ~/dotfiles/bash/common.sh

# ===== Macの場合 =====
if [ "$DOTFILES_MAC" = "true" ]; then
  # todo: Mac用の設定
fi

# ===== Host向けの設定 =====
if [ "$DOTFILES_HOST" = "true" ]; then
  # todo: Host向けの設定
fi
